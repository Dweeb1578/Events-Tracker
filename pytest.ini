[pytest]
# Only collect our own tests. The copied icp_classification/ contains standalone
# scripts (test_single.py etc.) that make real API calls at import time — never
# let pytest collect/import them.
testpaths = tests
norecursedirs = icp_classification .git output __pycache__
